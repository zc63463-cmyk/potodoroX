# PomodoroX 开发日志

## 2026-05-04 下午：任务规划与完成功能开发

### 新增功能：任务 + Session 双层规划/总结

**用户决策**：
- 任务状态流转时 + 番茄钟 Session 时都要支持文本记录
- 新增独立 `plan`/`completion` 字段（不复用 description）
- 任务详情弹窗（600px 单栏）展示规划/完成情况/专注记录
- 状态变更不阻塞，先标记完成再弹窗提醒补总结
- TimerView 内联轻量面板输入 Session 目标/总结

### 数据模型扩展

| 实体 | 新增字段 |
|------|---------|
| `Task` | `plan: string`, `completion: string` |
| `Session` | `plan: string`, `completion: string` |
| `CreateTaskInput` | 包含 `plan` + `completion`（默认空串） |
| `CreateSessionInput` | 包含 `plan` + `completion` |

### 数据库迁移

- **SQLite**: `runMigrations()` 使用 `PRAGMA table_info()` + `ALTER TABLE ADD COLUMN` 安全添加 4 个新列（`tasks.plan`, `tasks.completion`, `sessions.plan`, `sessions.completion`）
- **MemoryStore/IndexedDB**: `loadFromLocalStorage()` 中对旧数据做兼容性填充 `{ ...t, plan: t.plan ?? '', completion: t.completion ?? '' }`
- **localStorage 旧数据迁移**: 同样处理

### 新增组件

| 文件 | 职责 |
|------|------|
| `src/components/TaskDetailModal.vue` | 任务详情弹窗：顶部信息卡 + Tab 栏（规划/完成情况/专注记录）+ 800ms 防抖自动保存 |

### 修改模块

| 文件 | 改动 |
|------|------|
| `src/types/index.ts` | Task/Session 接口 + CreateTaskInput/CreateSessionInput 新增 plan/completion |
| `src/services/database.ts` | 迁移方法 + 所有 CRUD/upsert/rowToTask/rowToSession 更新 |
| `src/stores/session.ts` | 新增 `updateSession()` 方法（供 TimerView 补充 Session completion） |
| `src/stores/timer.ts` | 新增 `currentSessionPlan` 状态，`start(duration, plan?)` 签名，`completeSession()` 自动同步 plan 到 task.plan（如果 task.plan 为空） |
| `src/views/TasksView.vue` | 集成 TaskDetailModal，标题点击/详情按钮打开，toggleDone/moveTask 状态流转智能提示 |
| `src/views/TimerView.vue` | 内联 Session plan/completion 提示面板，开始 work Session 时询问目标，结束后询问总结 |

### 状态流转智能提示

- **待办 → 进行中**：如果 `task.plan` 为空，标记状态后立即打开详情弹窗到「规划」Tab
- **进行中 → 已完成**：如果 `task.completion` 为空，标记状态后立即打开详情弹窗到「完成情况」Tab
- **番茄钟开始**（work + 有任务）：内联面板询问「本次专注目标」（可选，可跳过）
- **番茄钟结束**（work）：内联面板询问「本次专注总结」（可选，可跳过）

### 自动同步策略

- 首次 Session 创建时，如果 `task.plan` 为空且 `session.plan` 有值，自动复制到任务规划
- 标记任务完成时，如果 `task.completion` 为空且最近 Session 有 completion，自动复制（通过 TasksView 调用）

### 验证
- 73/73 测试通过
- Vite 构建成功
- TypeScript 类型检查通过（遗留错误 4 个为 MagicRings/TimerView 已有问题）

---

## 2026-05-04 第四轮：Outbox 事件流架构（同步模型重构）

### 基于 Codex 深度审查的架构重构

**核心改动**：将 GitHub 同步从"覆盖 JSON 快照"模式改为"append-only 事件流"模式

### 新增模块

| 文件 | 职责 |
|------|------|
| `src/services/outbox.ts` | Append-only 事件队列。每条 CRUD 生成一个不可变事件（eventId + type + payload），本地 IndexedDB 持久化 |
| `src/services/event-consumer.ts` | 幂等事件消费者。从 GitHub 拉取 outbox 事件 → 按 eventId 去重 → 逐一写入本地 |

### 修改模块

| 文件 | 改动 |
|------|------|
| `src/services/github.ts` | 新增 `pushEvent()` / `pushEvents()` / `pullEvents()` — 推拉 outbox 事件（Append-only，永不覆盖已有文件） |
| `src/stores/sync.ts` | 全面重写：串行锁（serialized）、recordEvent()、backgroundPull()、outbox-based sync |
| `src/stores/task.ts` | CRUD 后调用 `recordEvent('task.created/updated/deleted', ...)` 写入 outbox 事件 |
| `src/stores/reflection.ts` | 同上（reflection.created/updated/deleted） |
| `src/stores/session.ts` | 同上（session.created） |
| `src/App.vue` | 启动时调用 `backgroundPull()` 消费远程事件 |

### 架构对比

```
之前（JSON 快照覆盖）:
  手机写 → 每月 tasks.json（覆盖）→ GitHub
  桌面写 → 每月 tasks.json（覆盖）→ 冲突

之后（Append-only 事件流）:
  手机写 → outbox/{eventId}.json（新建，永不覆盖）→ GitHub
  桌面写 → outbox/{eventId}.json（新建，永不覆盖）→ GitHub
  任何一端启动 → 拉取所有 outbox/*.json → 按 eventId 去重 → 幂等写入本地
  删除 → task.deleted 事件（tombstone 跨端可见）
```

### 关键质量属性

- **幂等消费**：同一 eventId 多次拉取只处理一次
- **串行安全**：全局 serialized 锁，防止并发同步冲突
- **无覆盖风险**：Append-only 模式，每个事件的 JSON 文件名带 eventId，永不覆盖
- **删除可传播**：delete 事件携带实体 ID，消费端执行本地删除
- **限流安全**：推拉各限制 10/100 个/批，避免 API 限频

### 验证
- 52/52 测试通过
- TypeScript 编译通过
- Vercel 部署成功
- 访问（旧）：https://dist-web-kohl.vercel.app（`dist-web` 为 CLI 自动创建的随机名项目）

### Vercel 部署修复

**问题**：`vercel --yes` 首次部署时自动创建了随机名项目 `dist-web`，而非用户已有的 `pomodorox-web`。`pomodorox-web` 显示 "No Production Deployment"。

**根因**：无 `.vercelignore` → CLI 尝试上传整个项目（含 `src-tauri/target/` 4.2GB）→ 部署失败 → CLI 回退创建新项目 `dist-web`。

**修复**：
- 创建 `.vercelignore`：排除 `node_modules/`, `src-tauri/target/`, `.git/`, `src/`, `scripts/` 等
- 创建 `vercel.json`：`{"buildCommand": null, "outputDirectory": "dist-web", "framework": null}` → 直接部署预构建产物，跳过云端构建
- 重新执行 `vercel --prod` → 成功部署到 `pomodorox-web`

**新生产地址**：
- https://pomodorox-web.vercel.app（推荐）
- https://pomodorox-meqwuxgs3-zc63463-8447s-projects.vercel.app（Vercel 自动域名）

### Phase 1 — Codex 同步健壮性问题修复（2026-05-04）

**P0 — recordEvent 无条件先写 outbox**
- `src/stores/sync.ts`: 移除 `if (!github.isConfigured()) return` 守卫，outbox 写操作为第一优先级
- `src/stores/sync.ts`: GitHub 未配置时事件仍然持久化到本地 IndexedDB，配置后自动推送

**P1 — CRUD 加 await recordEvent()**
- `src/stores/task.ts` 第13行: `recordEvent` 改为 `async` + 3 处调用加 `await`
- `src/stores/reflection.ts` 第12行: 同上（3 处）
- `src/stores/session.ts` 第13行: 同上（1 处）
- 确保 IndexedDB outbox 写入失败时 CRUD 操作会感知并抛出

**P1 — 后台推送/拉取走 serialized()**
- `src/stores/sync.ts`: `recordEvent` 中的后台 `syncPushEvents()` 用 `serialized()` 包裹
- `src/stores/sync.ts`: `backgroundPull()` 整体用 `serialized()` 包裹

**P1 — pushEvents 循环直到 outbox 清空**
- `src/stores/sync.ts`: `syncPushEvents()` 改为 `while(true)` 循环，每批 10 个事件推送，清空为止
- `src/services/github.ts`: `pushEvents()` 不再 slice(0,10)，由调用方控制批次

**P1 — pullEvents 全量拉取**
- `src/services/github.ts`: 移除 `items.slice(0, 100)` 限制，按文件名排序全量拉取
- `src/services/github.ts`: 添加 TODO checkpoint 增量拉取标记
- 消费端通过 `filterUnprocessed` 按 eventId 去重，避免重复消费

**验证**：52/52 测试通过，TypeScript 编译通过

### Codex Phase 2.5 — 消费者回归测试 + 时间比较最终修（2026-05-04）

Codex 第三轮审计要求补消费者链路测试，已全部完成：

**新增 event-consumer.test.ts（15 个测试）**
- `parseTime()`: ISO 格式、本地时间、Invalid Date 兜底
- `isNewerThan()`: 晚于/早于/相等/Invalid Date
- `consumeEvents()` 消费者链路（8 个测试）：
  - 未配置 GitHub 时跳过
  - 无远程事件返回空
  - 正常消费 task.created → upsert + markProcessed
  - 已处理事件跳过（filterUnprocessed）
  - 版本保护：旧 task.updated 不覆盖本地新数据 ✓（日志验证）
  - 墓碑保护：task.deleted 后旧 task.created 不复活 ✓（日志验证）
  - 删除后新 task.created 可恢复实体 ✓
  - 单个事件失败不影响后续

**已修复 bug（Codex 指出）：**
- `parseTime()` 先 `.replace(' ', 'T')` 再 `new Date()`，避免 iOS Safari 解析失败
- `markTombstone()` 新增 `timestamp` 参数，传入 `event.timestamp` 而非消费端本地时间
- 版本比较改用 `Date.getTime()` 毫秒值，不用字符串比

**验证**：73/73 测试通过，TypeScript 编译通过

### Phase 3 — 切 promoX-data 独立同步仓库（2026-05-04）

**改动**：
- `src/utils/constants.ts`: 默认 `githubRepo` 从 `pomodorox-data` → `promoX-data`，`githubOwner` 从空 → `zc63463-cmyk`
- `src/views/SettingsView.vue`: placeholder 从 `pomodorox-data` → `promoX-data`
- 内部目录 `GITHUB_SYNC_DIR = 'pomodorox-sync'` 保持不变（无需迁移）

**部署**：
- `pnpm run build:web` → Vercel 云端构建 → https://pomodorox-web.vercel.app（200 OK）
- 新用户首次打开设置页自动带出 `zc63463-cmyk/promoX-data`

**验证**：73/73 测试通过，TypeScript 编译通过，线上部署成功

**1. pushEvent 409 幂等**
- `src/services/github.ts`: Octokit 返回 409（文件已存在）时视为幂等成功，不再报错
- 修复场景：断网重试时远端已存在同 eventId 文件，或不同设备同时推同一事件

**2. 实体版本保护 + 墓碑模式**
- `src/services/outbox.ts`: 新增 `markTombstone()` / `getTombstone()` / `removeTombstone()` 墓碑追踪
- `src/services/event-consumer.ts`: 重写 `processEvent()`，添加 `shouldSkipDueToVersion()` 保护
  - 墓碑保护：实体被删除后，旧 created/updated 事件不会复活它（墓碑时间 > 事件时间则跳过）
  - 版本领先：本地实体 `updatedAt` > 事件 `timestamp` 时跳过（不覆盖新数据）
  - 删除事件自动记录墓碑，创建/更新事件自动清除墓碑

**验证**：52/52 测试通过，TypeScript 编译通过
