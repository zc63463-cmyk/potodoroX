# 2026-05-03 工作日志

## Bug 修复全量执行

根据 /project-workflow 流程 B，修复了诊断报告中的所有 8 个 Bug：

### P1 Bug 修复
- **#5 ReflectionsView 内存泄漏**: 添加 `onUnmounted` 清理 `autoSaveTimer`
- **#4 github.ts unescape()**: 替换为 `utf8ToBase64()` / `base64ToUtf8()` (TextEncoder/TextDecoder)
- **#6 StatsView 日期比较**: 字符串拼接改为 `new Date()` 对象比较（3 处 computed + exportReport）
- **#3 timerStore localStorage 绕过**: `getTotalDuration()` 和 `autoSwitchSession()` 改用 `useSettingsStore()`

### P2 Bug 修复
- **#8 isTauriAvailable 重复**: 创建 `src/utils/tauri.ts` 统一导出，4 处文件改为引用
- **#7 无效 Tauri invoke**: 移除 timerStore 和 useTimer 中所有 `invoke('timer_tick')`/`invoke('timer_stop')` 等无效调用

### P0 重构
- **#1 timerStore/useTimer 合并**: useTimer 标记废弃，timerStore 作为唯一计时器核心
- **#2 测试框架**: 配置 Vitest + happy-dom，编写 4 个测试文件

### 验证
- `npx vite build` 构建成功，无编译错误
- 新增测试文件: format.test.ts, export.test.ts, tauri.test.ts, github.test.ts

---

## Redesign 2.0 改造

### 计时器页面重构
- 底部统计栏改为控制按钮区（重置/暂停/跳过），带文字标签
- 统计信息改为底部信息栏，带图标（🍅/⏱️/🔥）
- 主按钮添加运行中脉冲动画

### Bug 修复
- **TimerStore 竞态条件**: 添加 `isCompleting` 锁，防止 `completeSession` 重复触发
- **ReflectionsView 自动保存**: 添加 `watch(content, triggerAutoSave)`，输入即自动保存

### 新功能
- **Toast 通知系统**: `src/components/ToastContainer.vue` + `appStore.showToast()`，替代所有 `alert()`
- **全局搜索**: `src/components/GlobalSearch.vue`，支持 Ctrl+K 快捷键，跨任务/反思搜索

### 代码改进
- `src/types/index.ts`: 新增 `ToastType` 类型
- `src/stores/app.ts`: 新增 `toasts` 状态、`showToast()` / `removeToast()` 方法
- `src/views/SettingsView.vue`: 3 处 `alert()` 替换为 `appStore.showToast()`
- `src/App.vue`: 挂载 `ToastContainer` 和 `GlobalSearch` 组件

---

## 测试环境修复

### 问题
运行 `pnpm test` 报错：`'vitest' 不是内部或外部命令`，`happy-dom` 未安装。

### 根因
`node_modules` 中确实缺少 `vitest` 和 `happy-dom`，尽管 `package.json` 中已声明。这是之前 `pnpm install` 未完整执行导致的。

### 修复
- 执行 `pnpm install`，成功安装 `vitest@3.2.4` 和 `happy-dom@17.6.3`
- 更新 7 个过期的测试断言以匹配重新设计后的代码：
  - `format.test.ts`: `getWeekdayName` 期望从简写（周日/周一）改为完整名称（星期日/星期一）
  - `export.test.ts`: 日报/周报标题、反思章节、概览章节的文本匹配更新

### 验证
- `pnpm test` 全部 33 个测试通过（4 个测试文件）

---

## Focus Flow Design System 全面重设计

### 设计系统核心
- **玻璃拟态 (Glassmorphism)**: `backdrop-filter: blur(20px) saturate(180%)` + 半透明背景 + 微妙边框
- **动态粒子背景**: 每个视图 2 个浮动 orb + radial-gradient 网格渐变
- **CSS 变量主题系统**: `--glass-bg`, `--glass-border`, `--glass-shadow`, `--accent-glow`, `--accent-dim`
- **动画系统**: `glow-pulse`, `gradient-shift`, `orb-drift`, `page-enter/leave`

### 全局改造
- `src/style.css`: 完全重写，新增玻璃拟态工具类、CSS 变量、动画关键帧
- `src/App.vue`: 侧边栏 → 底部 Tab 导航 + 顶部栏，SVG 图标，页面切换 Transition

### 各视图重设计（4 个代理并行）
- **TimerView.vue**: 双层环形进度（装饰环+虚线环+主环）、玻璃控制面板、动态背景
- **ReflectionsView.vue**: 玻璃编辑器面板、心情卡片渐变 glow、玻璃侧边栏、模态框 blur
- **StatsView.vue**: 玻璃统计卡片（icon glow）、渐变柱状图、甜甜圈图动画、折线图 glow
- **SettingsView.vue**: 玻璃设置区块、现代滑块（thumb glow）、开关（accent glow）、主题卡片 lift
- **TasksView.vue**: 玻璃头部/工具栏/看板列、任务卡片 hover glow、现代按钮、玻璃模态框

### 兼容性修复
- `src/style.css`: 添加 `--hover-bg` / `--active-bg` 兼容旧变量
- `src/views/StatsView.vue`: 修复代理引入的 `reduce((sum [, v])` 语法错误 → `reduce((sum, [, v])`

### 验证
- `pnpm run build` 构建成功
- `pnpm test` 33/33 测试通过
